### Orbs

Yet another file format to encode session data.

We need a way to save session data along with the rom itself.

Orb files are chunk based, we have different chunks, for instance:

META
Encodes session meta-data.

THMB
Stores a thumbnail image.

STAT
Encodes the emulator state.
